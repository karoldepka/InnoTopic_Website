// Stencil discovers @Component-decorated classes under src/ automatically; nothing to re-export
// yet - this file exists so tooling that expects a package entrypoint has one.
export type { StrokeSnapshot } from './components/doodle-canvas/doodle-canvas'
